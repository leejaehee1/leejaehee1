금일 진도는 RDS 마무리했고, s3 진도 나가는중


```
mysql -h rds-teacher.cluster-ro-c9kciawyijr0.ap-northeast-2.rds.amazonaws.com -uroot -prootroot

show databases;

create database test;

use test;

show tables;

create table course ( id integer, name varchar(255), createdAt datetime );

insert into course values (1, 'WEB', now());

select * from course;

# AWS CLI 설치 (Amazon Linux 2 기준)
sudo yum install -y awscli

# S3 버킷 목록 확인
aws s3 ls

# 파일 업로드
aws s3 cp sample.jpg s3://myapp-images-2025/
```

```
문제 1>
파일이름을 practice.txt 로 만들고,
내용을
Hello vi editor!
저장하고 나가고, cat 으로 확인

문제 2>
문서의 맨 마지막 줄 아래에 아래에 새로운 줄을 추가하고 다음 문장을 작성하라.

This is a new line.

문제 3>
첫 번째 줄을 복사한 뒤, 문서 맨 끝에 붙여넣기

문제 4>
전체 내용을 삭제하라.

mysql -h rds-teacher.cluster-ro-c9kciawyijr0.ap-northeast-2.rds.amazonaws.com -uroot -prootroot

show databases;

create database test;

use test;

show tables;

create table course ( id integer, name varchar(255), createdAt datetime );

insert into course values (1, 'WEB', now());

select * from course;

# AWS CLI 설치 (Amazon Linux 2 기준)
sudo yum install -y awscli

# S3 버킷 목록 확인
aws s3 ls

# 파일 업로드
aws s3 cp sample.jpg s3://myapp-images-2025/

npm i @aws-sdk/client-s3 @aws-sdk/s3-request-presigner multer mime-types

vi .env
PORT=3001

DB_HOST=rds-teacher.cluster-ro-c9kciawyijr0.ap-northeast-2.rds.amazonaws.com
DB_USER=root
DB_PASS=rootroot
DB_NAME=test

AWS_REGION=ap-northeast-2
S3_BUCKET=tmd-genai1-teacher
AWS_ACCESS_KEY_ID=AKIAZQ3DUUCHSLVJHNIB
AWS_SECRET_ACCESS_KEY=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

npm i dotenv

node server.js

# 다른 터미널을 열어서 실행한다.
echo "Hello" > test.txt

curl -X POST http://localhost:3001/s3/upload -F "file=@./test.txt"

curl "http://localhost:3001/s3/list?prefix=uploads/2025-08-21/"

sudo yum update -y
sudo yum install redis6 -y
redis6-cli
```
