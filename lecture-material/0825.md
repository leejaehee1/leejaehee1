오늘 수업 내용은 AWS serverless 화면을 보면서 API Gateway, Lambda 실습하고, 터미널에서 serverless 명령어를 실습하는 형태로 진행함
그리고 docker 와 관련하여 기본 명령어와 docker hub, dockerfile, docker compose 까지 실습함

```
aws 화면 확인
docker --version
open -a Docker
docker search nginx
pwd : /Users/jsoftware/dev/projects/leejaehee1/study/0825
mkdir dockerfile_dir

vi dockerfile
FROM node:24-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 3000
CMD ["node" , "server.js"]

npm init
package name: (dockerfile_dir) myapp
…
entry point: (index.js) server.js

디스코드의 server.js 코드 docker 실습 내용 반영

npm i
npm start
curl http://localhost:3000 (다른 터미널)

docker build -t myapp .

docker images
docker run -p 3000:3000 myapp
docker start <IMAGE_ID>
docker stop <IMAGE_ID>
docker run -it ubuntu bash

연습문제 : mysql/redis/postgres 설치하기

docker login
docker tag myapp john184/myapp:1.0
docker push john184/myapp:1.0

# docker-compose exam

vi docker-compose.yml
version: "3.8"

services:
  web:
    image: nginx:latests
    ports:
      - "8080:80"


docker compose up -d

vi docker-compose.yml
…
  mongo:
    image: mongo:6.0
    ports:
      - "27017:27017"
    volumes:
      - ./mongo-data:/data/db


docker compose up -d


# aws cli 설치
aws --region ap-northeast-2 iam get-user
aws s3 ls s3://tmd-genai1-teacher
npm install -g serverless

serverless --version

```
