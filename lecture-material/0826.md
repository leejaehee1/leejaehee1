severless 사용법 익혔음

```
Study/0826 생성

sls --version
sls deploy
sls invoke -f hello
sls logs -f hello

# 지정한 함수만 배포
sls deploy function -f hello

sls info
sls remove

# 소스코드는 GitHub 에 커밋함

curl https://van8jozwti.execute-api.ap-northeast-2.amazonaws.com/users/sadkfjlj\?userType\=testType

curl -X POST https://van8jozwti.execute-api.ap-northeast-2.amazonaws.com/users -H "Content-Type: application/json" -d '{"name": "Alice", "email": "alice@example.com"}'

curl -X PUT https://van8jozwti.execute-api.ap-northeast-2.amazonaws.com/users/13 -H "Content-Type: application/json" -d '{"name": "Alice", "email": "alice@example.com"}'

curl -X DELETE https://van8jozwti.execute-api.ap-northeast-2.amazonaws.com/users/111

npm init 
npm i uuid

npm i aws-sdk sharp

npm install --include=optional sharp
npm install --os=linux --cpu=x64


윈도우 환경 : 
npm install sharp --ignore-scripts
npm rebuild sharp --platform=linux --arch=x64 --libc=glibc
```
